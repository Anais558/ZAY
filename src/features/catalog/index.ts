/**
 * Catalogue produit : cartes, filtres, PLP — composants & queries à ajouter phase 1 suite.
 * Colocaliser ici tout ce qui dépend du domaine « produit » pour limiter le couplage UI générique.
 */
export type { Product, ProductListItem } from '@/types/product'
