import { PageHeader } from '@/components/common/PageHeader'

export function WishlistPage() {
  return (
    <div className="bg-background pb-16">
      <PageHeader title="Favoris" description="Wishlist persistée (Zustand + API utilisateur)." />
    </div>
  )
}
