import { PageHeader } from '@/components/common/PageHeader'

export function CartPage() {
  return (
    <div className="bg-background pb-16">
      <PageHeader
        title="Panier"
        description="Récap ligne items, codes promo, sticky summary mobile — phase 2."
      />
    </div>
  )
}
