/**
 * Domaine panier : point d’entrée public pour l’UI & hooks métier.
 * Le store canonique vit dans `store/useCartStore` pour éviter la duplication d’état.
 */
export { useCartStore } from '@/store/useCartStore'
